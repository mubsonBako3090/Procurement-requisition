# MongoDB
MONGODB_URI=your_mongodb_atlas_connection_string

# JWT
JWT_SECRET=replace_with_a_long_random_secret
JWT_EXPIRES_IN=24h

# App URL (used for password reset links, etc.)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Nodemailer (Gmail SMTP)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
SMTP_USER=your_gmail_address@gmail.com
SMTP_PASS=your_gmail_app_password
SMTP_FROM="KSU Procurement <your_gmail_address@gmail.com>"

# Cloudinary
CLOUDINARY_CLOUD_NAME=your_cloud_name
CLOUDINARY_API_KEY=your_api_key
CLOUDINARY_API_SECRET=your_api_secret

# Escalation threshold (Naira)
ESCALATION_THRESHOLD=10000000

# 32-byte base64 key used to encrypt the VC digital signature.
# Generate with: openssl rand -base64 32
SIGNATURE_ENCRYPTION_KEY=replace_with_base64_32_byte_key
